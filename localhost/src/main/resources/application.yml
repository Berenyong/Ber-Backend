spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost.cla2z9tnoqm3.ap-northeast-2.rds.amazonaws.com:3306/localhost?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: lej812200
    password: chldnjsdyd1
  profiles:
    include: oauth
  session:
    store-type: jdbc

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_batch_fetch_size: 1000

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace